package com.koudesuk.functionalstorage.client;

/**
 * Simplified client settings to mirror the old Forge config values.
 * These are constants on Fabric to keep renderer logic consistent.
 */
public class FunctionalStorageClientConfig {

    /** How far (in blocks) drawers render their contents. */
    public static final int DRAWER_RENDER_RANGE = 16;

    /** Thickness used when rendering 3D item/block displays on drawer fronts. */
    public static final double DRAWER_RENDER_THICKNESS = 0.125;
}
